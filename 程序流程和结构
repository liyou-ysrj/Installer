名词解释
数据块：要打包的文件由于有很多个，打包后的一个文件称为一个数据块
数据块打包类型：将要打包的文件放进包里有很多方法，不同的方法称为数据块打包类型

原理：
将要打包的数据放在解包程序尾部，解包程序运行时就自动去找自己尾部的数据，通过预定的流程将数据解包，按照打包时指定的命令将数据正确的解包安装。




数据块结构
Struct DataInfo
{      
	int data_dir_size;	//data_dir的长度
	char* data_dir;		//解包后数据块对应文件放置的目录，可以用相对路径也可以用绝对路径，在打包时给定
	int data_filename_size;	//data_filename的长度
	char* data_filename;	//解包后数据块的文件名，带文件类型后缀
	int data_type;		//打包时所用的方法
	__uint64_t data_len;	//data的长度
	char* data;		//数据块指针
}

一个打包后的程序有解包程序+数据块组成，同时在文件最后有一个__uint64_t类型的变量用来说明一共有多少个数据块。为了方便读取数据块，在打包时数据块是从解包程序尾部开始存入的，也就是说DataInfo结构在文件中的结构是这样的
文件头>>>>>>>>>>>>>>>>>>>>文件尾
data->datalen_len->data_type.....->data_dir_size

数据块的存入和读取都是用的binary模式



程序流程：
首先从解包文件尾部读取数据块总数，然后再接下来的解包中解包一个数据块就将这个数值递减，到它为0时就停止解包。解包时从文件尾向文件头一次读取DataInfo结构中数据成员的值.表示如下

1.读取数据块总数->2.从文件中读取内容填充datainfo结构->3.根据datatype解包->4.成功后判断是否读取完所有数据块，是的话继续重复步骤2，否则就退出
